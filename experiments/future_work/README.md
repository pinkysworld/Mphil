# Future-work experiments

This bundle covers the remaining thesis-aligned experiments that are feasible
with the current local workspace:

- held-out-family open-set evaluation
- archived token-level explanation case studies
- retraining-trigger analysis from the walk-forward profile

Run from the repository root:

```bash
bash scripts/run_future_work_bundle.sh
```

The external-validation suggestion is not run here because the workspace does
not currently contain an independent newer sandbox-report stream.
